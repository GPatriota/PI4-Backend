// DEPRECATED: This file is no longer used - migrated to SQLite with better-sqlite3
// See src/database/connection.ts for the new database connection

// import { PrismaClient } from '@prisma/client';
// 
// const prisma = new PrismaClient({
//   log: process.env.NODE_ENV === 'development' ? ['query', 'error', 'warn'] : ['error'],
// });
// 
// export default prisma;

// Temporary stub to prevent import errors until services are migrated
export default null as any;
